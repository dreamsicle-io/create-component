// @ts-nocheck

/**
 * _Template
 *
 * The `_template` function.
 *
 * @since _version Added by the `create-component` script on `_date`
 */
export default function _template() {
	const element = document.querySelector('._template');
	element?.innerHTML('Hello from the _Template script.')
}
